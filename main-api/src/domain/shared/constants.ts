// WORKSPACE
export enum WorkspaceTypes {
  ecommerce = "ECOMMERCE",
  appointment = "APPOINTMENT",
}

export enum WorkspaceStatus {
  active = "ACTIVE",
  inactive = "INACTIVE",
  cancelled = "CANCELLED",
}

// TOKEN
export enum TokenUsages {
  rememberPassword = "REMEMBER_PASSWORD",
  emailValidation = "EMAIL_VALIDATION",
}

export enum RoleTypes {
  home = "HOME",
}
